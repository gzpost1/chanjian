<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yjtech.wisdom.tourism.resource.depot.mapper.DepotStaySummaryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yjtech.wisdom.tourism.resource.depot.entity.DepotStaySummaryEntity">
        <id column="summary_id" property="summaryId" />
        <result column="time_name" property="timeName" />
        <result column="record_time" property="recordTime" />
        <result column="year" property="year" />
        <result column="month" property="month" />
        <result column="day" property="day" />
        <result column="hour" property="hour" />
        <result column="quantity" property="quantity" />
    </resultMap>

    <sql id="condition_sql_where">
        <if test="params.year != null">
            and year = #{params.year}
        </if>
        <if test="params.month != null">
            and month = #{params.month}
        </if>
        <if test="params.day != null">
            and day = #{params.day}
        </if>
        <if test="params.hour != null">
            and hour = #{params.hour}
        </if>
        <if test="params.beginTime != null and params.endTime != null">
            and record_time >= #{params.beginTime} and record_time <![CDATA[<]]> #{params.endTime}
        </if>
    </sql>

    <select id="queryStay" resultType="com.yjtech.wisdom.tourism.resource.depot.entity.vo.DepotStayVo"
            parameterType="com.yjtech.wisdom.tourism.resource.depot.query.DepotSummaryQuery">
        SELECT
            time_name name,
            COALESCE(sum( quantity ),0) AS quantity
        FROM
            tb_depot_stay_summary
        <where>
            <include refid="condition_sql_where"></include>
        </where>
        GROUP BY time_name
    </select>

</mapper>

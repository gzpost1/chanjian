apiVersion: apps/v1
kind: Deployment
metadata:
  name: standard-integrated-app-portal
  namespace: test-standard-integrated
  labels:
    app: standard-integrated-app-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: standard-integrated-app-portal
  template:
    metadata:
      labels:
        app: standard-integrated-app-portal
    spec:
      imagePullSecrets:
      - name: harbor-secret
      containers:
      - name: standard-integrated-app-portal
        args:
        - --spring.profiles.active=test
        image: ${CICD_IMAGE}:${CICD_EXECUTION_SEQUENCE}
        ports:
        - containerPort: 8090
---
kind: Service
apiVersion: v1
metadata:
  annotations:
    field.cattle.io/ipAddresses: "null"
    field.cattle.io/targetDnsRecordIds: "null"
    field.cattle.io/targetWorkloadIds: '["deployment:test-standard-integrated:standard-integrated-app-portal"]'
  name: standard-integrated-app-portal
  namespace: test-standard-integrated
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090